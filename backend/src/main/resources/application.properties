spring.application.name=backend

# Firebase configuration (using default credentials)
# No relational database is configured
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
